# ─────────────────────────────────────────────────────────────────────
# EarnStream — Environment Variables
# ─────────────────────────────────────────────────────────────────────
# Copy these into your Vercel project settings (Settings → Environment
# Variables) OR into a local .env file for development.
#
# The Supabase URL and anon key have hardcoded fallbacks in
# src/lib/supabase/server.ts, so the app will work even without these
# set. But it's best practice to set them explicitly.

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://rayhnuzdewzhtrwahzas.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_PgCe5mWQE2srbUnv-UcDpw_gHOcULV3
